<% @memberships.each do |membership| %>
  <table class="table table-striped">
    <caption>Course Title: <%= link_to membership.title, course %></caption>
    <thead>
      <th>Name</th>
      <th>Email</th>
      <th>Profession</th>
      <th>Address</th>
    </thead>
    <tbody>
      <% @memberships.select { |m| m.course_id == course.id }.each do |request| %>
        <tr>
          <td><%= request.user.getDisplayName %></td>
          <td><%= request.user.email %></td>
          <td><%= request.user.profession %></td>
          <td><%= request.user.address %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<style type="text/css">
    table tr td {
      padding-right: 5px;
    }

    table tr th {
      padding-right: 5px;
    }
</style>
