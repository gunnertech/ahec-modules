<%= form_for(setup_course(@course)) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :minimum_score %><br>
    <%= f.text_field :minimum_score, class: "form-control" %>
  </div>

  <div id="youtube-ids">
    <%= f.fields_for :youtube_video_ids do |ff| %>
      <div>
        <%= ff.label :URL %>
        <%= ff.text_field :video_id, class: "form-control" %>

        <% if ff.object.persisted? %>
          <%= ff.check_box :_destroy %>
          <%= ff.label :_destroy, "Delete?" %>
        <% end %>
      </div>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :questions %><br>
    <%= f.text_area :question_json %>
    <div id="course-question-editor">
      <input class="btn btn-primary" id="add-question" type="button" value="Add Question" onclick="addQuestion();"/>
    </div>
  </div>
  <div class="actions">
    <%= f.submit "Update Course", class: "btn btn-success" %>
  </div>
<% end %>
